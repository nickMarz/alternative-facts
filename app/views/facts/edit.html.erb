<div class="container edit-fact col-xs-12">
  <div id="notice"><%= notice %></div>
  <h1>Editing Fact</h1>

  <%= render 'form', fact: @fact %>

  <%= link_to 'Show', @fact %> |
  <button class="btn btn-default back-button"><%= link_to 'Back', facts_path %></button>
</div>
